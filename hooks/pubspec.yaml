name: flutter_web_preloader_hooks

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  mason: ^0.1.2
  yaml: ^3.1.2

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.8
  very_good_analysis: ^10.0.0
