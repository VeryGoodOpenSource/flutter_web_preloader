name: flutter_web_preloader_hooks

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  mason: ^0.1.2
  yaml: ^3.1.3

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.30.0
  very_good_analysis: ^10.2.0
